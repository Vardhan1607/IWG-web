/*Checking if loadash is working or not */
const _=require('loadash');
let example = _.fill([1,2,3,4,5],'apple',1,4)
console.log(example);
/*So as to install package xyz we use
npm install xyz
So as uninstall we use
npm uninstall xyz*/